# Validate required bands for specific indices

Validate required bands for specific indices

## Usage

``` r
validate_required_bands(
  index_type,
  blue,
  green,
  swir1,
  swir2,
  red_edge,
  red_edge2,
  coastal,
  nir2,
  verbose = FALSE
)
```
